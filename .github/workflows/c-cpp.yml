name: tests

on:
  push:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: build
      run: |
        ls
        ls third-party
        ls third-party/gtest
        make -H. -B_build -BUILD_TESTS=ON
        cmake --build _build
      
    - name: tests
      run: _build/check
